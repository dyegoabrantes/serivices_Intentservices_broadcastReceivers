O código nesse repositório compila uma aplicação android que executa atividades com intents,
intentservices, services, tasks e async tasks.

Falta implementar a notificação caso o estado da variável no banco mude.

Os services que executam a conexão devem receber como URL o endereço ip do servidor (cujo código
fonte se encontra na branch "perereServer").

